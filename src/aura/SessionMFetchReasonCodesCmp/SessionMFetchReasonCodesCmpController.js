({
    //This method is called when page loads initially
	 doInit: function(component, event, helper) {
            
            helper.fetchReasonCodePickListValues(component);

   }
                  
})